apiVersion: v1
kind: Secret
metadata:
  name: mongodb-secret
  namespace: meetup3
type: Opaque
stringData:
  # IMPORTANT: Change these values before deploying to production
  # Generate strong passwords using: openssl rand -base64 32
  mongodb-root-username: admin
  mongodb-root-password: Bfmdd1rCVQsEEYI76h4BseuzVBrokjfN
  mongodb-database: honey-store
